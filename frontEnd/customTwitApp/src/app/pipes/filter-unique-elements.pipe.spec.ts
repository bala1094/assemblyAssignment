import { FilterUniqueElementsPipe } from './filter-unique-elements.pipe';

describe('FilterUniqueElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUniqueElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
